name: enrich-30f7ee54-fit
# Throwaway. Do not merge. Not hunt.yml. Re-enrich 30f7ee54 after PR #9.
on:
  workflow_dispatch:
  push:
    branches: [enrich-30f7ee54-fit]
jobs:
  enrich:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: bb5d5b7740d445decaf47d020ea7fb42241bef4c
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install requests==2.33.1
      - name: Enrich edition 30f7ee54 only
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python hunt_runner.py --enrich-edition 30f7ee54
